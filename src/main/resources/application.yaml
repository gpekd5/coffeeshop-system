spring:
  application:
    name: coffee-order-system
  profiles:
    default: local
  jackson:
    time-zone: Asia/Seoul
  jpa:
    open-in-view: false

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

app:
  time-zone: Asia/Seoul
